apiVersion: courier.sororlab.dev/v1alpha1
kind: OAuthClient
metadata:
  name: report-exporter
  namespace: team-bravo
  labels:
    courier.sororlab.dev/client: report-exporter
  annotations:
    # Rotate once per distinct value.
    courier.sororlab.dev/rotate: "2026-09-14-phase4-demo"
spec:
  displayName: "team-bravo: nightly report exporter"
  clientType: confidential
  grantTypes:
    - client_credentials
  scopes:
    - openid
    - profile
    - groups
