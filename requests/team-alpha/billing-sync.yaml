apiVersion: courier.sororlab.dev/v1alpha1
kind: OAuthClient
metadata:
  name: billing-sync
  namespace: team-alpha
  labels:
    courier.sororlab.dev/client: billing-sync
spec:
  displayName: "team-alpha: billing sync job"
  clientType: confidential
  grantTypes:
    - client_credentials
  scopes:
    - openid
    - profile
    - groups
